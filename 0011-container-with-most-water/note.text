11. Container With Most Water
題目概述： 給一組高度陣列，選兩條線圍成容器，求最大盛水面積。
解題邏輯： 左右雙指標從兩端開始，面積 = (r - l) * min(height[l], height[r])，每次更新最大值。移動較矮那邊的指標，因為寬度一定縮小，只有換掉矮邊才有機會讓高度變大來補償。
踩過的坑： 無，直覺解。
複雜度： O(n) time / O(1) space