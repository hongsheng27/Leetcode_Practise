104. Maximum Depth of Binary Tree
題目概述： 給一棵二叉樹的 root，回傳樹的最大深度（root 到最遠葉子的節點數）。
解法 1：DFS 遞迴（由下到上）
解法 2：BFS（按層計數）

核心：
1. dfs: 子樹回傳深度，當前層取較大值 + 1。由下到上最純粹的模板。
bfs: 每輪 for 迴圈清空當前層，range(len(queue)) 固定當前層大小，不受新加入的 node 影響。
複雜度： O(n) time / O(h) space