680. Valid Palindrome II
題目概述： 給一個字串，最多刪除一個字元，判斷是否能成為回文。
解題邏輯： 左右雙指標往中間靠，相同就繼續。遇到 s[l] != s[r] 時，不猜方向，直接用 helper 驗證 (l+1, r) 和 (l, r-1) 兩段，任一個是回文就 return True。helper 就是標準雙指標判斷回文。
踩過的坑：

一開始想貪心 peek 下一格決定刪左或刪右，但局部資訊不夠判斷（如 eceec 兩邊都能 match 下一格，只有一邊是正解）。正確做法是兩條路都完整跑完再判斷。

複雜度： O(n) time / O(1) space