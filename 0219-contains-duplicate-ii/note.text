
LeetCode 219 - Contains Duplicate II
======================================

題目：給一個陣列 nums 和整數 k，判斷是否存在兩個不同的 index i, j，
使得 nums[i] == nums[j] 且 abs(i - j) <= k。

Fixed Sliding Window 經典題。

核心思路：
  維護一個大小最多為 k 的 HashSet 當作窗口。
  從左往右掃，每次檢查新元素是否已經在窗口裡（代表 k 範圍內有重複）。
  窗口超過 k 時，把最左邊的元素移出。

  HashSet 的作用：O(1) 檢查「窗口內有沒有重複」。
  固定窗口大小 k：保證找到的重複一定在 k 的距離內。


複雜度：O(n) 時間，O(k) 空間。


# 補充：
HashMap 也可以完成。

HashMap 解法：
記錄每個數字最近一次出現的 index。
如果目前 index 和上次 index 的差 <= k，就回傳 True。

但 HashMap 最壞會存下所有不同元素：

Space: O(n)

本題更好的做法是 sliding window + HashSet。

因為只需要關心距離目前 index 不超過 k 的元素，
不需要保留完整歷史紀錄。

